# 360度写真 & 動画ビューワー 操作マニュアル

## 1. はじめに

「360度写真 & 動画ビューワー」をご利用いただきありがとうございます。
本ツールは、360度カメラで撮影されたパノラマ画像や動画を、パソコンのブラウザ上で手軽に閲覧できるアプリケーションです。

### このツールでできること

- **360度画像・動画の閲覧**: 全方位を見渡せるパノラマ形式で表示します。
- **アノテーション機能**: 画像上に「ここがポイント」といったテキストラベルや、注目箇所を示す矢印を配置できます。
- **画像の保存**: テキストや矢印を書き込んだ状態で、画像ファイルとして保存（エクスポート）できます。

---

## 2. 入手方法と起動方法

本ツールはインストール不要で、1つのファイルをダウンロードするだけですぐに使えます。

1. **ダウンロード**: GitHub の Releases ページから `index.html` というファイルをダウンロードします。
2. **起動**: ダウンロードした `index.html` をダブルクリックします。
3. **完了**: お使いのブラウザ（Google Chrome や Microsoft Edge など）が立ち上がり、ビューワーが表示されます。

---

## 3. 画面の見方

起動すると、画面下部に操作のための「ツールバー」が表示されています。左から順に以下の機能が並んでいます。

1. **ファイル選択ボタン**: 画像や動画ファイルを読み込むためのボタンです。
2. **テキストツール領域**（画像表示時のみ）: テキストを入力したり、文字の色を選んだりする場所です。
3. **動画コントロール**（動画表示時のみ）: 再生・一時停止や巻き戻しなどの操作ボタンです。
4. **編集プロパティパネル**（アノテーション選択時のみ）: 配置したテキストや矢印の色・サイズなどを変更するパネルです。
5. **視点初期化ボタン**: 視点（カメラの向き）を最初の状態に戻します。
6. **画像保存領域**（画像表示時のみ）: 画像の形式（JPEG/PNG）を選んで保存するボタンです。

---

## 4. 画像を表示する

1. 画面下部の「**ファイルを選択**」ボタンをクリックします。
2. パソコン内のフォルダから、表示したい360度パノラマ画像を選択し、「開く」をクリックします。
   - **対応している画像形式**: JPEG (`.jpg`, `.jpeg`), PNG (`.png`), GIF (`.gif`), BMP (`.bmp`)
3. 画面全体にパノラマ画像が表示されます。

※ 新しく別のファイルを選択すると、前に表示していた画像は消え、新しい画像に切り替わります。

---

## 5. 視点を操作する

マウスを使って、360度自由に視点を動かすことができます。画像・動画共通の操作です。

| したいこと | マウス操作 |
| :--- | :--- |
| **周りを見回す**（回転） | 左ボタンを押しながらドラッグ（上下左右に動かす） |
| **拡大・縮小**（ズーム） | マウスホイールを回す |
| **視点の位置をずらす**（パン） | 右ボタンを押しながらドラッグ |

- **視点を元に戻したいとき**: 画面下部の「**視点初期化**」ボタンをクリックすると、最初の向きとズーム倍率に戻ります。

---

## 6. テキストラベルを追加する

画像上の好きな場所に、説明書き（テキストラベル）を配置できます。（※動画では使えません）

1. 画面下部のテキスト入力欄（「ラベルを入力」と書かれた場所）に、表示したい文字を入力します。
2. 必要に応じて、右隣のカラーピッカー（色のついた四角）をクリックして文字色を選びます（初期値は白です）。
3. 「**テキスト: OFF**」ボタンをクリックします。ボタンの表示が「**テキスト: ON**」に変わり、入力モードになります。
   - カーソルの形が変わります。
4. 画像上の文字を置きたい場所をクリックします。
5. クリックした場所にテキストが配置されます。
6. 続けて別の場所をクリックすると、同じ文字を連続して配置できます。
7. 配置を終了するには、「**テキスト: ON**」ボタンをもう一度クリックして OFF にするか、キーボードの `Escape` キーを押します。

---

## 7. 矢印を追加する

注目してほしい場所を指し示す矢印を配置できます。（※動画では使えません）

1. 必要に応じて、カラーピッカーで矢印の色を選びます（初期値は赤です）。
2. 「**矢印: OFF**」ボタンをクリックします。ボタンの表示が「**矢印: ON**」に変わり、入力モードになります。
   - カーソルが十字の形に変わります。
3. 画像上で、矢印の**始点**（根元）をクリックし、そのまま**終点**（先端）までドラッグ（マウスボタンを押したまま移動）します。
4. ドラッグ中は矢印のプレビューが表示されます。マウスボタンを離すと矢印が確定します。
5. 配置を終了するには、「**矢印: ON**」ボタンをもう一度クリックして OFF にするか、キーボードの `Escape` キーを押します。

---

## 8. アノテーションを編集・削除する

配置したテキストや矢印（これらをまとめて「アノテーション」と呼びます）は、後から位置や色を変更したり、削除したりできます。

### 選択方法

1. テキストモードや矢印モードが「OFF」になっていることを確認してください。
2. 画面上の編集したいアノテーションをクリックします。
3. アノテーションに「**ギズモ**」と呼ばれる赤・緑・青の矢印が表示され、画面下部に「編集プロパティパネル」が現れます。

※ **ギズモとは**: 物体を移動させたり回転させたりするための操作ハンドルのことです。

### 編集できること

画面下部のパネルで以下の操作が可能です。

- **移動**: 画像上のギズモ（赤・緑・青の矢印）をドラッグすると、その方向にアノテーションを移動できます。
- **回転**（矢印のみ）: パネルの「回転」ボタンを押すと、ギズモが回転用の形に変わります。ドラッグして向きを変えられます。「移動」ボタンで移動モードに戻ります。
- **テキスト変更**（テキストのみ）: パネル内の入力欄で文字を書き換えられます。
- **サイズ変更**（テキストのみ）: スライダーを動かして文字の大きさを調整できます（0.5倍〜5.0倍）。
- **色変更**: カラーピッカーで色を変更できます。
- **削除**: 赤色の「**削除**」ボタンをクリックすると、選択中のアノテーションが消えます。

編集を終えるときは、パネルの「**閉じる**」ボタンを押すか、画像の何もない場所をクリックして選択を解除してください。

### すべて削除する

画面下部の「**全消去**」ボタンをクリックすると、配置されているすべてのアノテーションが一括で削除されます。

---

## 9. 画像を保存する

アノテーションを追加した状態の画像を、新しいファイルとしてパソコンに保存できます。（※動画では使えません）

1. 画面下部のドロップダウンメニューから、保存したい形式（**JPEG** または **PNG**）を選びます。
2. 「**画像保存**」ボタンをクリックします。
3. ブラウザのダウンロード機能により、画像ファイル（`equirectangular.jpg` または `equirectangular.png`）が保存されます。

※ **保存される画像について**:
保存される画像は「エクイレクタングラー形式」と呼ばれる、360度全方位を1枚の長方形に展開した画像です。Facebook や対応するビューワーなどで360度画像として認識されます。解像度は読み込んだ元の画像と同じです。

---

## 10. 動画を再生する

「ファイルを選択」ボタンで動画ファイル（MP4, WebM, FLV）を選ぶと、動画モードになります。

### 再生コントロール

画面下部のボタンやキーボードで動画を操作できます。

| 操作 | 画面上のボタン | キーボード / マウス |
| :--- | :--- | :--- |
| **再生 / 一時停止** | 再生（▶） / 一時停止（⏸）ボタン | `Space` キー または 画面クリック |
| **10秒戻る** | 巻き戻し（⏪）ボタン | `←`（左矢印）キー |
| **10秒進む** | 早送り（⏩）ボタン | `→`（右矢印）キー |

※ 動画モードでは、テキストや矢印の追加、画像の保存機能は使用できません。

---

## 11. よくある質問（FAQ）

**Q. ファイルを選択しても画面が真っ暗で何も表示されません。**
A. 選択したファイルが対応形式（画像: JPEG/PNG/GIF/BMP、動画: MP4/WebM/FLV）かご確認ください。また、ファイルが壊れていないか、別のビューワーで開けるかお試しください。

**Q. テキストを配置しようとしても何も起きません。**
A. 以下の2点をご確認ください。
1. テキスト入力欄に文字が入力されているか（空欄だと配置できません）。
2. 「テキスト: OFF」ボタンをクリックして「**テキスト: ON**」の状態になっているか。

**Q. 配置したテキストや矢印をクリックしても選択できません。**
A. 「テキスト: ON」または「矢印: ON」の状態（入力モード）になっていませんか？ 入力モード中は新しい配置が優先されるため、既にあるものを選択できません。ボタンをクリックして「**OFF**」にしてから再度クリックしてください。

**Q. 動画を表示中ですが、テキスト入力や画像保存のボタンが見当たりません。**
A. 動画モードでは、アノテーション機能（テキスト・矢印）と画像保存機能は無効になるため、それらのボタンは非表示になります。

**Q. 保存した画像に、操作用のハンドル（ギズモ）も一緒に写ってしまいませんか？**
A. いいえ、写りません。保存処理を行う際、ギズモは自動的に非表示になりますので、ご安心ください。

---

## 12. 動作環境

- **OS**: Windows, macOS, Linux など
- **ブラウザ**: Google Chrome, Microsoft Edge, Firefox, Safari（いずれも最新版を推奨）
  - ※ Internet Explorer では動作しません。
